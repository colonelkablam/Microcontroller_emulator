; text preceded with a ';' is ignored - useful for comments and clarification

main:	; a word at beginning of the line is treated as a label (semicolon needed after)

	GOTO main		; GOTO will jump to either an explicit address ('0x06') or label)
